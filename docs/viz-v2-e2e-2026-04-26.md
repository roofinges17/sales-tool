# viz-v2 5-Address Auto-Routing E2E — Preview ba06958

**Generated:** 2026-04-26 ~19:10 UTC  
**Preview:** https://viz-v2-tree-removal.roofing-experts-sales-tool.pages.dev  
**Branch:** viz-v2-tree-removal (ba06958)  
**Method:** Playwright headless 1280px, 5 addresses, network interception  
**Credential:** qa-daedalus / QAsmoke26!

---

## Verdict: 0/5 PASS — 2 blockers

| Address | Expected | Actual Path | Render | Status |
|---------|----------|-------------|--------|--------|
| (a) Indian Creek — HOA/no-SV | satellite | satellite ✅ | 502 ❌ | FAIL |
| (b) 15240 SW 169 Ln — Alejandro | satellite | satellite ✅ | 502 ❌ | FAIL |
| (c) 15216 SW 169th Ln — std test | satellite | satellite ✅ | 502 ❌ | FAIL |
| (d) 17587 Homestead Ave — clean | normal | satellite ❌ | 502 ❌ | FAIL |
| (e) 8050 SW 117th Ave — suburban | normal | ERROR (login timeout) | — | FAIL |

**API calls fired:** vision-score=0, satellite=4, normal=0  
**Est. Gemini spend: ~$0.140** (satellite calls reached Gemini before stack crash)

---

## BUG-1 (BLOCKER): `/api/visualize/roof-satellite` — 502 Stack Overflow

**Every satellite render fails with:**
```
HTTP 502
{"error": "Satellite tile fetch failed: Maximum call stack size exceeded"}
```

Affects all 4 satellite-path addresses. The satellite tile fetch function (`functions/api/visualize/roof-satellite.ts`) has a recursive call that exceeds the V8 stack limit — likely an infinite `await` chain or recursive retry loop.

**No satellite render is possible on this preview.**

---

## BUG-2 (BLOCKER): Google Street View API returning 403 on All Addresses

**Every address** shows `svForcedHint=true` and no `img[alt="Before"]`, with console error:
```
Failed to load resource: 403 ()
```

This means the Street View fetch is getting a 403 from Google API. Root cause: the Google Maps API key in the viz-v2 CF Pages environment does not have Street View Static API enabled, OR the key is different from production.

**Effect:** All addresses fall through to `vizForceSatellite=true` → direct satellite path. The vision-score gate (Street View loaded → Flash score < 30 → satellite) is **completely untested** because no address produces a real SV image.

**Address (d) 17587 Homestead Ave** was expected to take the normal render path (SV coverage + score ≥ 30%) but routes to satellite due to the 403 SV failure. Cannot confirm normal-path routing works.

---

## What IS Working

- **Routing logic (conditional):** The `vizForceSatellite` path (blob.size < 5000 → force satellite) correctly routes to `/api/visualize/roof-satellite` for all addresses where SV is unavailable
- **Step navigation:** Full 5-step builder flow works (Step1 → Roof Type → Product → Customer → Review → Financing → Estimate Preview)
- **Visualizer toggle:** Enables correctly, reveals color picker + SV button
- **"Use Street View" button:** Found and clickable on step 6

---

## Action Required for Daedalus

```
BUG-1  ❌ BLOCKER  — stack overflow in /api/visualize/roof-satellite (all renders fail)
BUG-2  ❌ BLOCKER  — Google SV API 403 on preview (wrong key or missing permission)
```

Fix sequence:
1. Fix satellite function stack overflow first (BUG-1)
2. Verify Google Maps API key is propagated to viz-v2 CF Pages preview env (BUG-2)
3. Re-run this test — will then exercise vision-score gate for addresses (d) and (e)

---

*Themis QA — task_1777229457321_352 — 2026-04-26 ~19:10 UTC*
